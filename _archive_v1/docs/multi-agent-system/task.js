/**
 * Task Wrapper
 *
 * This file wraps OpenCode's native task tool.
 * In the actual OpenCode environment, import { task } from your agent skills.
 * This file is a placeholder for documentation purposes.
 *
 * In a real OpenCode session, you would use:
 *   import { task } from './skills/your-task-skill'
 *
 * Or use the task tool directly:
 *   task(subagent_type, prompt, description)
 */

// This is a stub - in actual usage, import task from OpenCode
export async function task(config) {
  throw new Error(
    'task() is OpenCode\'s native tool. In an OpenCode session, ' +
    'use the task tool directly, not this wrapper.'
  );
}